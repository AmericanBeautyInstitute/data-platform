data_models:
  target: prod
  outputs:
    prod:
      type: bigquery
      method: service-account
      project: american-beauty-institute
      # Fallback dataset only; models route to raw/staging/marts via +schema.
      dataset: staging
      keyfile: "{{ env_var('GOOGLE_APPLICATION_CREDENTIALS') }}"
      location: US
      threads: 4
